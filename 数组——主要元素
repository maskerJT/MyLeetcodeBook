步进法：O（nlogn）
找主要元素的方法是先对数组进行排序，排序之后如果满足主要元素的条件，那么必然有
数组中的某一个元素=数组中该元素+一半步长后位置的元素值。
该方法主要的时间消耗在排序算法上，vector的一个简单排序为：
sort(nums.begin(),nums.end())

摩尔投票法：O(n)
重要的是count的确定，初始化众数等于第一个数组元素，对于数组中的某元素：
  当现有的元素与上一个元素相等时，count++；
  当现有的元素与上一元素不相等时，count--；
  判断到现有元素时的count是否为0，为零则更换count的对象，同时将此时的count置位为1；




  
