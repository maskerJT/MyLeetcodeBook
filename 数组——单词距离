暴力解法中，使用两个vector来保存两个单词的出现位置，然后通过暴力检索这两个位置vector的的最小距离。

好一点的解法中，可以不必要保存两个位置vector，因为这两个位置vector内元素的大小是递增的，给了我们局部比较最佳距离的机会。
###可以只用一个vector，最好遍历一遍这个vector，返回最小的索引距离就可以了！

###没必要索引的其实，直接使用出现的新下标减去另一个字符的上一个下标，然后再用这个差值对比之前存储的最小值即可。
